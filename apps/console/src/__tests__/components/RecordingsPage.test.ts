import { describe, it, expect } from 'vitest';
import RecordingsPage from '@/pages/RecordingsPage.vue';

describe('RecordingsPage', () => {
  it('should be importable', () => {
    expect(RecordingsPage).toBeDefined();
  });
});
