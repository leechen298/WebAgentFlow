prompt_id = "learning_agent"
version = "v1"
agent_role = "learning_agent"
schema_name = "LearningAgentRequest"
runtime_policy = "request_skill_only"
allowed_inputs = ["validated_target", "goal", "slots", "page_understanding", "session_context"]
forbidden_outputs = ["selector", "playwright_step", "browser_action", "learned_path_id"]
sensitive_context_policy = "redacted_only"
