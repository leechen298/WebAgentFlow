# M11.3.6 Runtime Eval Program Closeout Rerun

## Summary

- Status: blocked / implementation_review_failed
- Commit: `77790f6`
- Date: 2026-05-23T09:57:09Z
- API Base: `http://127.0.0.1:8001`
- Product URL: `http://127.0.0.1:<fixture-port>/records`
- Closeout package: `docs/iterations/m11/11.3.6.5-runtime-eval-program-closeout/`

The required evals were rerun after API and fixture-site became reachable. Both commands
left the previous preflight-blocked state and executed through the Conversation API, but both
returned exit `1` because required gates failed. The raw runner artifacts from this rerun are
not committed because redaction inspection found a full private path id in the planner result.

## Commands

| Command / Surface | Expected | Actual | Exit code | Result |
|---|---|---|---:|---|
| API health | API / DB available | HTTP 200, `database=ok` | 0 | pass |
| product `/records` | page available | HTTP 200 | 0 | pass |
| `pnpm run eval:wagent:pending-choice` | all required gates pass | `status=fail`; required gates `13/14` | 1 | fail |
| `pnpm run eval:wagent:planner-choice` | all required gates pass | `status=fail`; `planner_backed_choice` failed; `planner_single_path_bypass_regression` passed | 1 | fail |
| strict private-id grep | no full private path id | full private path id found in planner raw output | 0 | fail |

## Package Status

| Package | Status | Evidence |
|---|---|---|
| 11.3.6.1 Runner Core | implemented_and_live_eval_passed | Existing result `docs/testing/results/m11-11.3.6.1-wagent-runtime-eval-core-20260522T162710Z.md` |
| 11.3.6.2 Failure Recovery Eval | implementation_complete_non_live | Existing result `docs/testing/results/m11-11.3.6.2-failure-recovery-eval-20260522T163632Z.md` |
| 11.3.6.3 Pending Choice Eval | implementation_review_failed | Service-available rerun failed `public_choice_payload_sanitized` |
| 11.3.6.4 Planner-backed Choice Eval | implementation_review_failed | Service-available rerun failed planner-backed choice execution / verification gates; single-path bypass regression passed |
| 11.3.6.5 Program Closeout | blocked | This result |

## Failure Summary

- 11.3.6.3: `public_choice_payload_sanitized` failed because public surfaces exposed the
  private pending-choice token name `learned_path_id`.
- 11.3.6.4: planner candidates and public choices were created and choice A dispatch succeeded,
  but `chat_execution_started` was missing after selection. The selected-choice execution path,
  result verification, and final response verification gates failed.
- 11.3.6.4 regression: `planner_single_path_bypass_regression` passed.
- Redaction: the raw planner rerun output contained a full private path id, so those raw artifacts
  must not be submitted as program evidence until the runner redaction is fixed or the artifacts are
  regenerated safely.

## Boundary

- Live Conversation eval: ran for the required eval commands and failed required gates.
- Console UI smoke: not run.
- `verify-scenario`: not run.
- Autonomous run endpoints: not called.
- Direct replay substitution: not used.

## Final Recommendation

Do not mark M11.3.6 as complete. Open a code-type fix iteration that covers both runtime gate
failures and runner artifact redaction, then rerun:

```bash
pnpm run eval:wagent:pending-choice
pnpm run eval:wagent:planner-choice
```
